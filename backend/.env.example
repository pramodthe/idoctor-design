# iDoctor Design secrets — never commit real keys
#   cp backend/.env.example backend/.env
# .env is gitignored. This file is the committed template.

# --- Claude (critic / experiment prose) ---
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# --- Paperclip (literature) — CLI + API key, optional PAPERCLIP_BASE_URL ---
PAPERCLIP_API_KEY=
PAPERCLIP_BASE_URL=

# --- Tamarind (structure fold/complex jobs) ---
TAMARIND_API_KEY=

# --- Modal (Proto GPU) ---
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# Benchling: ignored — Monday card is markdown only (backend/agents/experiment.py).

# --- Proto ---
USE_PROTO=0
PROTO_BINDER_LENGTH=48
PROTO_NUM_SAMPLES=4
PROTO_NUM_RESULTS=4
PROTO_DEVICE=cuda
IDOCTOR_DESIGN_LIVE_DESIGN=1

IDOCTOR_DESIGN_DEFAULT_MODE=fixture
DEFAULT_PDB_ID=6OIM
